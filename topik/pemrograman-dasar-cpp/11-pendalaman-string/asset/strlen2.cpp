#include <cstdio>
#include <cstring>

char s[1001];

int main() {
  scanf("%s", s);
  for (int i = 0; i < strlen(s); i++) {
    // 
  }

  int len = strlen(s);
  for (int i = 0; i < len; i++) {
    // 
  }
}